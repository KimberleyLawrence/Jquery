$(document).ready();

// $() = tells jQuery that things are about to happen//
// document = between the () tells us that we are about to work our magic on the html document itself.//
// .ready = is a function, or basic action, in jQuery. It's saying that as soon as the html doc is ready it will do things.//
// .ready() = whatever goes into .ready's () is the jQuery event that occurs as soon as the html doc is ready.//
// ; = tells jQuery that you are done giving a command. //